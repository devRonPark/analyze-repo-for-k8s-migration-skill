# Current Status

| Work item | Status | Evidence | Blocker |
| --- | --- | --- | --- |
| PIPE-000 — runtime binding compatibility | DONE | Installed `@opencode-ai/plugin` 1.18.14 declares `ToolContext.sessionID` separately from model-call arguments; the evidence and limitation are recorded in `daily/2026-08-08/PIPE-000-completion-2026-08-08.md`. ADR-2026-08-08-007 removes final-response interception from this gate. | None. |
| PIPE-001 — PIPE-005 — trusted analysis pipeline | TODO | Resequenced in ADR-2026-08-08-005; content integrity is the required acceptance boundary per ADR-2026-08-08-007. | Start PIPE-001's pure state-machine and deterministic vertical proof. |
| INT-001 — help before analysis | DONE | Help and usage routing contract is covered by target safety tests and the Quality Gate. | None. |
| INT-002 — local target resolution | DONE | Worktree-bounded target contract is covered by target safety tests and the Quality Gate. | None. |
| INT-003 — report-only response | PARTIAL | Harness tests and Quality Gate pass; TTY run loaded the Skill and agent without mutating the target. | Final TTY Markdown report was not captured. |
| DET-001 — Detailed evidence slots | DONE | JPetStore 6 Detailed E2E returned a complete eight-section report in 2m 14s within the read budget and left the target Git status unchanged. | None. |
| DET-004 — verdict consistency | DONE | The validator accepts a repeated identical verdict, rejects differing ones, and runs keyed-blocker validation again. | None. |
| DET-005 — report line shapes | DONE | Detailed E2E rerun completed a 117-line report in 1m 56s; validator failures fell from 45 to 14 and keyed-blocker violations to 0, with the target unchanged. | None. |
| DET-006 — absence and conflict evidence | DONE | The DET-006 rerun completed a 124-line report in 2m 24s with no translated `검색` marker and no unparsable `상충됨` source; `미확인` evidence without `검색(...)` fell from 5 to 1. | None. |
| DET-007 — evidence references | DONE | The DET-007 rerun completed a 139-line report in 2m 1s with zero citations outside the repository; validator failures fell from 21 to 14. | None. |
| DET-008 — invented line ranges | DONE | The DET-008 rerun completed a 130-line report in 2m 28s with no out-of-range or reversed line span, no missing section, and validator failures down to 7. | None. |
| DET-009 — false absence evidence | DONE | The DET-009 rerun completed a 125-line report in 2m 5s with zero false `result=없음` claims; `image` is a fact again and the application-server dependency edge is present. | None. |
| DET-003 — Detailed golden-set scoring | PARTIAL | The DET-009 report scores 72/100 against the independent JPetStore golden set, up from 63. | Below the 90-point threshold, and the five-candidate food-delivery target does not exist on this machine. |
| DET-010 — DET-014 — remaining golden-set deductions | TODO | Recorded with per-ticket evidence in TICKET-LIST-2026-07-30-008; the 2026-07-30 report is retained at `tests/evaluation/jpetstore-6-detailed-e2e-2026-07-30.md`. | Instruction-only fixes show diminishing returns; a validate-and-repair loop needs an ADR first. |
| SEC-001 — safe evidence boundary | IN_PROGRESS | The trusted `read`, `glob`, and `git_metadata` tools loaded in the JPetStore E2E with no `grep`, `list`, or `bash` call and no credential literal in the report. | Adapter unit and canary E2E coverage are not implemented. |
| TKT-001 through TKT-005 | DEFERRED | Urgent-plan slices remain recorded in the prior ticket list. | Not the active queue. |
| Initial OpenCode-only milestone | INTERRUPTED | Original progress ledger is preserved in [`../archive/initial-opencode-milestone/status.md`](../archive/initial-opencode-milestone/status.md). | Superseded by the urgent priority. |
| DEL-002 — DEL-003 — deterministic Summary delivery | DONE | Implemented per [ADR-2026-08-07-001](../daily/2026-08-07/ADR-2026-08-07-001-summary-delivery-structured-output.md): Summary Agent is JSON-only; `run_opencode_acceptance.py`'s `retain_summary_markdown` renders via `render_summary.py`, validates, and finalizes the receipt. Live-verified against `demo-repositories/jpetstore-6` with the real provider: `isolated/repeat-01/slash-default-summary: PASS`, target repository unchanged. | DEL-004 (permanent E2E gate/artifact retention) and the sglang `response_format` schema constraint remain open; JSON emission is still prompt-only, not engine-enforced. |
| VS-020 — Summary v2 field coverage | DONE | Golden-set score for `demo-repositories/jpetstore-6` Summary went 58/100 → 84/100 by routing build/image-alignment risk and Secret exposure into `missing_inputs` (already renderable) instead of leaving them in unrendered component `fields`. No renderer/template/schema change. See `tests/evaluation/jpetstore-6-summary-json-first-scorecard.md`. | VS-021 (recommendation-classification leak) and VS-022 (engine-enforced JSON) remain open. |
| VS-021 — reject `recommendation` in Summary | DONE | `report_contract.validate_json_payload` now rejects a `recommendation`-classified `missing_inputs` entry when `mode == "summary"`; Detailed mode unaffected (dedicated test). | None. |
| VS-022 — sglang `response_format` constraint | CLOSED (investigated, not implemented) | OpenCode 1.18's `format: json_schema` structured output is real but only reachable via SDK `session.prompt()`, not `opencode.json`, an agent `.md`, or any CLI path this project uses. Switching the inference engine (sglang → vLLM) would not close this gap — the block is OpenCode's invocation surface, not engine capability. | Closing this for real needs an SDK-based invocation architecture (`run_opencode_acceptance.py` and eventually the documented user workflow), which is a new ADR-scale decision, not a config change. |
| Summary validate-and-repair loop | DONE | Per [ADR-2026-08-07-002](../daily/2026-08-07/ADR-2026-08-07-002-summary-validate-and-repair-loop.md): `run_case` now captures `trace["session_id"]`; `retain_summary_markdown_with_repair` retries a named validation error in the same session (bounded, default 2 repairs) via `opencode run --session <id>`. Live-verified: a run that failed on the first attempt is not yet observed passing via repair (both live checks today passed on attempt 1, so the repair path itself ran only under unit tests with a fake runner). | Live evidence that a real failure actually gets repaired successfully is still open — worth confirming opportunistically on a future run that fails on attempt 1. |
| SEC-002 — `read` tool symlink-escape gap | DONE | `read.ts`'s `safePath()` and `glob.ts`'s `safeRoot()` now call the new shared `runtime/lib/safe-path.ts`'s `isSafeWithin()`, which resolves symlinks/junctions on both root and candidate before the boundary comparison. Reproduced and regression-tested with a directory junction (real file symlinks need elevated privilege this machine doesn't have, same constraint as VS-019); 7/7 pass via `bun test runtime/lib/safe-path.test.ts`. Since live-verified indirectly: the VS-023 live runs exercised `isSafeWithin()` through real `read`/`glob` calls (3 runs, no path-escape, no crash). See [SEC-002](../tickets/SEC-002-read-tool-symlink-escape.md). | None. A dedicated live symlink-escape attempt (not just incidental exercise) is still untried, but not blocking. |
| VS-023 Phase 1 — `locate_evidence` sensor tool | DONE | Live-verified: 3/3 `opencode` runs against `jpetstore-6` pinned to the golden set's revision, all `PASS`, target unchanged, 0 fabricated `file:line` citations across 30 evidence citations checked (`locate_evidence` called 7/10/13 times per run). Evidence-calibration dimension re-scored 9/10 (was 8/10 post-VS-020). Follow-up same session: the agent prompt now requires every `근거:`/`reference` to be a `locate_evidence`-sourced single `path:line` (no hand-written citations, no `path:start-end` ranges); re-verified over 4 more live runs (3 PASS, 1 timeout), 0 hand-written citations observed post-enforcement vs. 2 self-computed ranges pre-enforcement. See `tests/evaluation/jpetstore-6-summary-json-first-scorecard.md`'s "VS-023 Phase 1 live verification" section and the VS-023 ticket's "Live verification outcome" / "Citation-sourcing enforcement follow-up" sections. Phase 2 (five ecosystem-aware `inspect_*` tools) stays deferred per ADR-2026-08-07-003. All of the above is Summary mode only. | A residual, non-fabrication precision gap remains: `locate_evidence` returns the *first* pattern match, so an under-specific `pattern` can still cite a real but topically-imprecise line (observed twice: a Spanish doc file, and a non-credential `INSERT` row). Also fixed in this pass: `install-opencode.sh`/`run_opencode_acceptance.py` were missing a `runtime/lib/` copy step (broke every live tool invocation since SEC-002), and the repair loop crashed instead of failing gracefully on a repair-session timeout — both have regression tests now. `steps: 32` was doubled to `64` to give the enforced-`locate_evidence` citation flow more room; measured Summary timing against `jpetstore-6` went from ~179.4s avg (steps:32) to ~231.5s avg (steps:64, 122.8-426.1s range) — one slow run used the extra room for 158 `locate_evidence` calls and caught a real compatibility finding (Spring 6.x vs. `javax.*` `web.xml`) no prior run this session had evidenced. See VS-023 ticket's "Step-budget increase and timing measurement". Not yet verified for Detailed mode — see VS-024 row below, whose interactive run found a wrong-file citation this Summary-only verification cannot speak to. |
| VS-024 — Detailed mode JSON-first pipeline | PARTIAL (implementation done, live verification deferred) | Both commits landed on `worktree-review-main`: schema extension + `render_detailed.py` + `report_contract.py`'s `detailed_json_errors` (commit `8dce923`), then agent-prompt replacement + `run_opencode_acceptance.py` harness generalization (commit 2) — TDD throughout, 178 tests passing (1 pre-existing unrelated VS-019 failure, confirmed via `git stash`). Also fixed a gap the generalization surfaced: `validate_target_report.py`'s `finalize()` hardcoded Summary's `--mode`/receipt-line assumptions. See the ticket's "Decision outcome (partial)" section. | Live verification (3+ `slash-detailed` repeats against `jpetstore-6`, before/after validator counts and rubric score) is deferred — the local OpenCode provider (`http://172.16.4.249:30000/v1`) was not reachable this session; user will provide access separately. Until then, `render_detailed.py`'s output is verified against `validate_report.py` by unit test only, not by a live model-authored payload. Separately, the interactive run found a wrong-file citation problem (`docker-compose.yaml:17`/`:21` cited for Dockerfile-sourced claims, repeated 3x/4x) that VS-024 explicitly does not fix — it is a content/evidence-sourcing risk in the same class as VS-023, not a rendering problem. |

Allowed status values: `TODO`, `IN_PROGRESS`, `DONE`, `PARTIAL`, `BLOCKED`,
`DEFERRED`, `NEEDS_TICKET`, `INTERRUPTED`.
