# Configuration Timing

For each material configuration, distinguish build, deployment, process-start,
runtime, and externally managed timing. Record its component, purpose, source
or injection method, change effect, and secret classification without exposing
values. A frontend build argument is not automatically a process-start value.
