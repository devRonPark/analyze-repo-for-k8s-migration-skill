# Detailed Analysis Checklist

In Detailed mode, every deployable candidate and exclusion must have a terminal
report slot for execution, configuration timing, state, dependency, lifecycle,
and readiness evidence. Keep install, build, image, startup, and operating
environment facts distinct. Unknown inputs name the scope and blocked or open
decision; do not create defaults.
